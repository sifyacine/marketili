import React from "react";
import SharedCalendar from "../shared/SharedCalendar";

const FreelancerCalendar = ({ user }) => <SharedCalendar user={user} role="freelancer" />;

export default FreelancerCalendar;
