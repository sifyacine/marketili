import React from "react";
import SharedCalendar from "../shared/SharedCalendar";

const DirectorCalendar = ({ user }) => <SharedCalendar user={user} role="agency" />;

export default DirectorCalendar;
